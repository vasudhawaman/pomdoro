import React ,{useContext} from 'react';
import {ThemeContext} from '../context';

function Sound(){
    const theme = useContext(ThemeContext);
    function findAudio(theme){
        switch(theme){
            case 'beach' :
                return 
        }
    }
     let audioURL = findAudio(theme);
}